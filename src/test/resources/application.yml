# TEST 용 spring boot 는 test 시 기본적으로 메모리DB 사용

spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리모드
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #application 실행 후 마지막 drop
#    properties:
#      hibernate:
#        #show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace